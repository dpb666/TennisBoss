package com.tennisboss.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * TennisBoss Palette — Silicon Valley High-Tech (2025)
 * Style : Dark-first, neon signals, data-driven.
 */

// Backgrounds
val TechBackground = Color(0xFF0A0E14) // Bleu profond presque noir
val TechSurface = Color(0xFF131A24)    // Cartes
val TechSurfaceVariant = Color(0xFF1C2530) // Cartes élevées / Hover

// Signals (Neon)
val TechPrimary = Color(0xFF00E5A0)   // Vert néon "Signal" — Favoris, Value+
val TechSecondary = Color(0xFF4F8CFF) // Bleu électrique — Accents
val TechDanger = Color(0xFFFF5C7A)    // Rouge néon — LIVE, Value négative
val TechWarning = Color(0xFFFFB020)   // Ambre — Confiance faible

// Text & Borders
val TechOnSurface = Color(0xFFE6EDF3)    // Texte principal
val TechOnSurfaceDim = Color(0xFFB0BEC8) // Texte secondaire
val TechOutline = Color(0xFF637585)      // Bordures fines 1dp
